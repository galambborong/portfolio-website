import { RouteComponentProps } from '@reach/router';
import React from 'react';

const LandingPage = (props: RouteComponentProps): JSX.Element => {
  return <p>Hello this is working</p>;
};

export default LandingPage;
